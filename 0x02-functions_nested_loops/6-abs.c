#include "main.h"

/**
 * _abs -  a function that computes the absolute value of an integer.
 * @i: is an integer to check
 *
 * Return: absolute value of an int
 */

int _abs(int i)
{
	if (i > 0)
	{
		return (i);
	}
	else
	{
		return (-i);
	}
}
